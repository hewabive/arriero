# vLLM 0.26.0 qualification

Status: managed environment and static CUDA checks passed; live-engine
qualification is in progress.

Date: 2026-07-30 (UTC).

## Host

| Component | Value |
| --- | --- |
| OS | Ubuntu 24.04, Linux x86-64 |
| CPU | AMD EPYC 7402P, 8 visible cores, AVX2, one visible NUMA node |
| RAM / swap | 32 GiB / 4 GiB |
| GPU | NVIDIA RTX A5000, 24 GiB, compute capability 8.6 |
| NVIDIA driver | 595.71.05 |
| CUDA toolkit | 12.0.140 |

The system toolkit is not the vLLM runtime. The managed PyPI environment
resolved PyTorch's bundled CUDA runtime independently.

## Managed environment

Arriero provisioned the official `vllm==0.26.0` PyPI wheel transactionally as
environment `019fb3fc-9670-7567-898b-782542e46bc5`. The job passed:

1. uv-managed CPython resolution and relocatable venv creation;
2. dependency resolution and package installation;
3. canonical installed-version inventory generation;
4. atomic publication;
5. exact package metadata and module-version validation.

The environment and its generated path-catalog entry report
`installed / usable`. The relevant resolved runtime is:

| Component | Version |
| --- | --- |
| CPython | 3.12.13 |
| vLLM | 0.26.0 |
| PyTorch / bundled CUDA | 2.11.0 / 13.0 |
| Transformers | 5.14.1 |
| Triton | 3.6.0 |

`torch.cuda.is_available()` is true and reports the RTX A5000 as `sm_86`.

## Argument catalog

`vllm serve --help=all` completed successfully in 22.619 seconds and produced
115,991 bytes of live help. Arriero parsed 280 options from the authoritative
help instead of serving its conservative fallback. The catalog includes the
manager-critical host, port, served-model name, GPU utilization, model length,
maximum sequences, tensor-parallel size, and reasoning-parser options.

## Planned live profile

The live gate uses `Qwen/Qwen3-4B` on GPU 0 with tensor parallelism 1. The
profile intentionally caps model length, batch concurrency, and GPU memory
utilization so the 24-GiB device retains a measurable safety margin.

The remaining gate covers direct and proxied OpenAI protocols, the Anthropic
bridge, streaming, configured concurrency, process-tree memory accounting,
active and idle shutdown, manager adoption, failed-start diagnostics, and
autostart through the proxy.
