# KTransformers 0.6.4 qualification

Status: qualified with the host and artifact restrictions recorded below.

Date: 2026-07-30 (UTC).

Arriero qualification implementation tip: `b940dc1`.

## Host

| Component | Value |
| --- | --- |
| OS | Ubuntu 24.04, Linux x86-64 |
| CPU | AMD EPYC 7402P, 8 visible cores, AVX2, one visible NUMA node |
| RAM / swap | 32 GiB / 4 GiB |
| GPU | NVIDIA RTX A5000, 24 GiB, compute capability 8.6 |
| NVIDIA driver | 595.71.05 |
| CUDA toolkit | 12.0.140 |
| Python | CPython 3.12.13 |

## Package pair

Both roots use the current upstream version `0.6.4`, released on 2026-07-23.

| Distribution | Artifact | SHA-256 |
| --- | --- | --- |
| `kt-kernel` | `kt_kernel-0.6.4-cp312-cp312-linux_x86_64.whl` | `f96de0b5cb06a3059b6f7342080fbbf2b481e1bc06129e07b136039a45775c35` |
| `sglang-kt` with upstream RoPE fix | `sglang_kt-0.6.4-py3-none-any.whl` | `7d9a32e236424b156060fd6ef82cc437948e7fa0e70916b831622bde08ab3365` |

The `kt-kernel` wheel was built from official tag `v0.6.4`, source commit
`a8062bfa7e1060ce5855b5f1ad6aa6b116678307`, with:

- `LLAMA_NATIVE=OFF`;
- AVX2 enabled;
- AVX-512, AMX, BLIS, and KML disabled;
- CUDA enabled only for architecture 86;
- release optimization and LTO disabled.

Loading this wheel against the target dependencies and creating and
destroying `kt_kernel_ext.CPUInfer(1)` succeeded. The public
`kt-kernel==0.6.4` wheel selected its advertised AVX2 extension on the same
host but terminated with `SIGILL` during that smoke test. The locally built
wheel is therefore required on this host.

The published `sglang-kt==0.6.4` wheel, SHA-256
`e31ab761dc5243964c598be58f7aad549318662d3789855c79a7c4d095270173`,
omits upstream commit `04653fa` (`fix(rope): restore rotated key write-back`).
That defect leaves the current-attention key unrotated while writing the
rotated key only to the cache. A controlled full-model run returned HTTP 200
but semantically corrupted tokens.

The qualified candidate repacks the same pure-Python wheel after applying
only the two runtime files changed by upstream commit `04653fa`. SHA-256 for
each patched file matches the corresponding file at that upstream commit:

| File | SHA-256 |
| --- | --- |
| `sglang/jit_kernel/rope.py` | `7adc2e070e1dd611308a36b7a8900709d3c59d5cf9193a53a36972345cf1d89c` |
| `sglang/jit_kernel/csrc/elementwise/rope.cuh` | `d8ad71c51c050e3e8db85f5f956827b51cd6dba507c14258b19bb630d9bdf4dc` |

## Managed environment

Arriero provisioned the two local artifacts transactionally as environment
`019fb3c5-84cf-7d59-af61-53e9887de844`. The job passed:

1. CPython resolution and relocatable venv creation;
2. local artifact SHA-256 verification;
3. one dependency-resolution/install transaction;
4. canonical installed-version inventory generation;
5. atomic publication;
6. exact root metadata/import validation and `CPUInfer(1)` execution.

The resulting environment and its generated path-catalog entry are both
`installed / usable`. The relevant resolved runtime is:

| Component | Version |
| --- | --- |
| CPython | 3.12.13 |
| PyTorch / bundled CUDA | 2.9.1 / 12.8 |
| `transformers-kt` | 5.6.0.post1 |
| `flashinfer-python` / `flashinfer-cubin` | 0.6.3 / 0.6.3 |
| Triton | 3.5.1 |

## Models

| Role | Revision / artifact |
| --- | --- |
| Main model | `Qwen/Qwen3-30B-A3B` at `ad44e777bcd18fa416d9da3bd8f70d33ebb85d39` |
| CPU weights | `Qwen3-30B-A3B-Q4_K_M.gguf` from `Qwen/Qwen3-30B-A3B-GGUF` at `e4d4bafdfb96a411a163846265362aceb0b9c63a` |

The GGUF SHA-256 is
`0d003f6662faee786ed5da3e31b29c978de5ae5d275c8794c606a7f3c01aa8f5`.
An independent CPU run with upstream `llama.cpp` commit
`e1a1abb78746c025f5e9039f590e37ccdb758ae7` produced a coherent continuation
for the qualification prompt, confirming that the downloaded GGUF is valid.

## Superseded 0.6.3.post1 result

The `0.6.3.post1` server reached HTTP 200 and served all tested OpenAI
endpoints, but generated semantically corrupted tokens. The failure remained
with:

- CUDA graph, radix cache, and deferred experts disabled;
- zero or 32 GPU experts;
- one CPU pool or two pools mapped to the single NUMA node;
- both the pinned `transformers-kt` package and standard
  `transformers==4.57.1`.

This excludes the model artifacts, Transformers warning, CUDA expert path,
deferred scheduling, and NUMA pool count as primary causes. Version
`0.6.3.post1` is not qualified for inference on this host.

## Live baseline

The patched `0.6.4` pair reached `/health` and `/v1/models` HTTP 200 in
5 minutes 39 seconds with the conservative profile:

- one KTransformers CPU pool using all 8 visible CPU cores;
- 32 GPU experts and no deferred experts;
- CUDA graph and radix cache disabled;
- two running requests and 8,192 total scheduled tokens;
- 0.8 static GPU-memory fraction.

The first semantic request, which also compiled the patched JIT kernel,
completed in 76.7 seconds. It produced coherent reasoning about `2 + 2`
instead of the corrupted output from the unpatched wheel. Deterministic
`/no_think` probes then returned exactly:

| Probe | Result | Wall time |
| --- | --- | --- |
| `2 + 2` | `4` | 3.63 s |
| `7 * 6` | `42` | 4.47 s |

Direct protocol checks passed for non-streaming and streaming
`/v1/chat/completions` and for `/v1/responses`. Two concurrent chat requests
returned the correct values `12` and `14` in 5.67 seconds total.

Arriero published the instance as `qwen3-30b-a3b-kt`. End-to-end requests
through the manager proxy passed:

| Proxy protocol | Expected / observed | HTTP |
| --- | --- | --- |
| OpenAI Chat Completions | `10` / `10` | 200 |
| OpenAI Responses | `proxy-ok` / `proxy-ok` | 200 |
| Anthropic Messages | `8` / `8` | 200 |

## Qualified tuned profile

The retained instance profile is:

| Setting | Value |
| --- | --- |
| Method | `LLAMAFILE` |
| `CUDA_VISIBLE_DEVICES` / TP | `0` / `1` |
| CPU workers / pools | `8` / `1` |
| GPU experts | `32` |
| Maximum deferred experts per token | `2` |
| Static GPU-memory fraction | `0.8` |
| Maximum running requests | `2` |
| Total scheduled tokens | `8192` |
| Chunked prefill | `2048` |
| CUDA graph | enabled, maximum batch size `2` |
| Radix cache | enabled |
| Declared host / GPU draw | 27.0 GiB / 23.0 GiB |
| Scheduling | `idle-only` |

This profile reached HTTP readiness in 6 minutes 10 seconds. CUDA graph
capture for batches 1 and 2 took 47.9 seconds, consumed 0.14 GiB, and left
5.36 GiB available on the GPU. A deterministic direct probe returned `42`.
Two concurrent proxied probes returned `22` and `32`; repeated warm short
requests completed in 4.7–5.4 seconds.

At the final measurement the complete process tree used about 18.3 GiB of
device memory and 19.6 GiB of anonymous host RAM, with another 11.3 GiB of
file-backed mappings. About 2.8 GiB of the process tree was swapped. The
manager therefore correctly reports `degraded` even though `/health` and
`/v1/models` are HTTP 200 and semantic requests pass. The remaining log warning
is the upstream Transformers 5.x compatibility warning; a controlled
Transformers 4.57.1 run did not repair the unpatched RoPE failure.

## Lifecycle and manager integration

- The public proxy model is `qwen3-30b-a3b-kt` and reports `loaded`.
- Direct and proxied streaming/non-streaming OpenAI requests, Responses, the
  Anthropic bridge, and the configured concurrency boundary passed.
- During a live proxied request the manager observed one active lease. A manual
  stop interrupted the request with a controlled HTTP 502 and removed the
  root, descendants, listener, and GPU process within one second.
- Two API hot reloads adopted the already-running KTransformers root without
  duplicating it. A proxied semantic request passed after each adoption.
- Optional DeepSeek-V4 import failures are now notices rather than false Qwen
  runtime errors. The SGLang listener parser reports the actual Uvicorn URL,
  not later documentation URLs from performance warnings.
- The host exposes only one NUMA node, so a real multi-NUMA placement test is
  not possible here. Single-pool placement and strict multi-node preflight
  validation passed.
- Production self-update is intentionally unavailable under `pnpm dev`, and
  no production supervisor was placed in scope. Manager reload/adoption and
  active process-tree shutdown cover the local lifecycle work possible here.

## Verdict

The exact artifact pair, Qwen model revisions, and tuned LLAMAFILE profile in
this document are qualified on this host. Public `kt-kernel==0.6.4` and the
unpatched public `sglang-kt==0.6.4` wheel are explicitly not qualified here.
Other CPU ISAs, native/converted weight methods, multi-GPU, and multi-NUMA
topologies require their own recorded qualification.

Repository verification at `b940dc1` passed:

- `pnpm check`;
- `pnpm build` (only the existing Vite large-chunk warning);
- `pnpm --filter @arriero/api test`: 941 tests, 0 failures;
- the detached KTransformers process-tree test passed five consecutive runs.
